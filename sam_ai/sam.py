def get_answer(query):
    """
    Generate answer, sources, and image_url from the database based on the given query.
    For the Frontend sam-ui, the query will be the user's input.
    Args:
        query (str): The query for fetching data from the database.
    Returns:
        tuple: A tuple containing the answer, sources, and image_url, or None if not found.
    """

    return ['fake answer', 'fake sources', 'fake image_url']